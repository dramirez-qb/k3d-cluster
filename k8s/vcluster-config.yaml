# kubectl get secret vc-${VCLUSTER_NAME} -n vcluster-${VCLUSTER_NAME} --template={{.data.config}} | base64 -d
syncer:
  extraArgs:
  - --tls-san=${VCLUSTER_NAME}.${SECRET_DOMAIN},${CURRENT_IP}
  - --out-kube-config-server=https://${VCLUSTER_NAME}.${SECRET_DOMAIN}
fallbackHostDns: true
sync:
  ingresses:
    enabled: true
